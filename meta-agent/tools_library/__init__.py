"""Pre-built tool library for sub-agents.

Each module exports:
  - TOOL_CODE: str — Python code for the @tool function(s)
  - TOOL_NAMES: str — comma-separated function names
  - TOOL_META: dict — metadata for the UI (name, description, category)
"""
