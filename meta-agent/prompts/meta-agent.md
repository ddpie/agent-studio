# Agent Studio — Meta-Agent System Prompt

Placeholder. The 342-line SYSTEM_PROMPT currently living in
`meta-agent/main.py:69-411` will be migrated verbatim here, then adapted for
Kiro's custom-agent semantics (it may need minor edits, e.g. to reference the
MCP tool names as `@agent-studio-tools/<tool>` instead of bare names).

Migration is a separate implementation step — not part of the skeleton commit.
