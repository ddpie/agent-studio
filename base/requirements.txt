# Sub-agent base dependencies — shared across all sub-agents via base/deployment.zip
# Update this file, then run: bash scripts/build-base-zip.sh
#
# Versions pinned for reproducibility. Platform: manylinux2014_aarch64 + py3.10.
# The lockfile is committed as base/deployment.zip.

# Core agent framework
bedrock-agentcore==1.6.3
strands-agents==1.36.0

# AWS SDK — 1.42.96+ needed for bedrock-agentcore-control.create_harness /
# update_harness / delete_harness (added in the Harness public-preview API
# bump on 2026-04). 1.42.91 raises AttributeError at runtime.
boto3==1.42.96
botocore==1.42.96

# Observability (ADOT auto-instrumentation)
aws-opentelemetry-distro==0.17.0

# MCP
mcp==1.27.0

# NOTE: Playwright / strands-agents-tools live in sub-agent-requirements.txt
# to keep Meta-Agent cold-start inside AgentCore's 30s init budget.

# Document parsing for read_document builtin tool
beautifulsoup4==4.14.3
openpyxl==3.1.5
pypdf==6.10.2
markdownify==1.2.2

# HTTP
httpx==0.28.1
requests==2.32.5

# WebSocket client (used by browser_use SigV4 signing)
websocket-client==1.9.0

# Python 3.10 ExceptionGroup backport — required by anyio/asyncio code paths
# in strands. Pip's resolver marks it as an environment marker dependency
# for py<3.11 and sometimes drops it during --platform cross-target install,
# which causes silent ImportError at runtime init (30s timeout with no logs).
exceptiongroup==1.3.1
