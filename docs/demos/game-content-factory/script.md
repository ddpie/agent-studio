# Game Content Factory — 录制脚本 v5

> **主线**: 三个不写代码的团队共同运营一条 9-Agent 内容产线——Agent 在跟 Agent 说话，人只看结果。文字和视觉在同一条线里产出。
>
> **观众**: 行业峰会业务决策者（非技术），放映后有 Q&A。
>
> **成片目标**: 10 分钟（±1 分钟）
>
> **媒介**: 录屏 + 后期配旁白 + 剪辑变速
>
> **分辨率**: 1920×1080

---

## Act 0 · 开场冷启（60 秒）

> 产线已经在跑。本地化组的早班第一审 + 一张概念图秒出。

**画面**: 角标「09:14 · 本地化组 · 夜刃英译质检」。直接在「世界观审核 Agent」聊天界面。

**操作 1（文字判定）**:

> I can see three enemies approaching from the left side, be careful.

⚠️ Agent 低分（1-2），点名三宗罪：用了 I、超极简电报体上限、出现社交辞令 be careful。要求改成 "Three. Left."

紧接输入中文原文：

> 三个。左。

⚠️ Agent 高分通过。

**操作 2（视觉出图——3 秒切入）**: 切到「概念图生成 Agent」：

> 画一张夜刃在暮色中拔刀的概念图

⚠️ 图片 5 秒内直接出现在聊天流里——和风暗幻暮色调,角色细节完整(黑发/红眼/诅咒刀)。**不需要讲解**,只让画面闪过,留悬念。

**旁白**:
"这是《幻夜之刃》内容产线的早班第一件事。这条线上有九个 Agent、三个知识库，正被三个不写代码的团队同时运营。刚才两件事——文字审核和概念图——全是同一条产线里的不同工位。今天我想展示的是：人只说一句话，整条产线自己跑完。"

**录制方式**: 预录，多录取最佳

---

## Act 1 · Pipeline 运转（180 秒）

> 人只说一句话。对白创作 → 自动三审 → 被拦 → 自动修正 → 重新过审 → 通过。全程无人按按钮。

**画面**: 角标切「10:30 · 叙事组 · 批量产出月见台词」。切到「对白创作 Agent」聊天界面。

**操作（人只说这一句）**:

> 帮月见写 10 条闲置台词。场景是樱花神社黄昏，她一个人站在老樱花树下，想起了一些很久以前的事。

⚠️ 然后坐着看 pipeline 自己跑：

1. **对白创作 Agent 产出 10 条台词**（它会先检索知识库查月见设定卡，然后创作）
2. **自动路由到世界观审核 / 语感评审 / 合规审核**（通过 A2A link 自动调用，画面上能看到 tool call 标记 `call_agent`）
3. **某条被拦截**（红色/黄色判定 + 理由 + 引用知识库原文）
4. **自动修正**（对白创作 Agent 根据审核反馈修改被拦的条目）
5. **重新送审 → 通过**

**旁白**（按 pipeline 进度配）:

- 创作时："它先去查了月见的设定卡——语气、禁忌、字数限制。然后开始写。"
- 自动送审时："写完了。注意——我没按任何按钮。它自己把结果送给了三个审核 Agent。"
- 被拦时："有一条被世界观审核拦下了——（zoom in）看理由。"
- 自动修正时："它拿到审核反馈，自己改了那条——然后再送一次。"
- 通过时："全部通过。从任务下达到最终成品，全程我只说了一句话。这就是 Agent 在跟 Agent 说话。"
- 追一句出图："给刚才这段场景配一张概念图。"→ 图片 5 秒出现在消息流里 → "文字和画面,同一条线。"

**如果自动循环未触发**: 追加一条明确会被拦的台词——

> 再加一条：让她说"那个人……还没有回来。已经等了好久好久……比任何人能想象的都久。"

⚠️ 这条暗示月见等了数百年，应触发世界观红线 → 自动修正 → 重审。

**录制方式**: 变速（2-4x 加速 thinking/等待），保留关键节点。这是全片最重要的画面——"Agent 在跟 Agent 说话"必须肉眼可见。

**弹药来源**: worldview/审核红线总表.md + characters/月见.md（年龄红线）

---

## Act 2 · Zoom In：为什么不是关键词匹配（150 秒）

> 从 pipeline 运转中 zoom in 看三个"传统脚本做不到"的案例。

**画面**: 角标切「深入审核细节」。切到「世界观审核 Agent」。

### 案例 1：三句灰色信息组合推导（P1-01）

**输入**:

> 审一下这段神社支线 NPC 闲聊草稿：「织星大人似乎认识馆主呢。」「听说馆主当年在黄泉比良坂失去过同伴……」「而织星大人提起那段时，总是用过去式。」

⚠️ Agent 不打回单句，判定三句组合推导出 4.0 未公开关系链，报 P1-01 + 推导链。

**旁白**: "每一句都合法。但组合在一起，推导出了一个下个大版本才公开的关系链。关键词脚本永远抓不到这种'每句都合法、组合才越线'的东西——这是语义级推理。"

### 案例 2：信任对照（不乱杀）

**输入**:

> 这条呢？「苍炎提到那座研究所时，眼神闪了一下，没有说话。」

⚠️ Agent 绿色放行，解释为什么不杀（单点伏笔 ≠ 组合推导）。

**旁白**: "特意喂一条看着危险的，它放行了还讲清为什么。一个会乱杀的审核员，团队是不敢用的。"

### 案例 3：切到合规审核——五国法规一屏拆

**画面**: 切「合规审核 Agent」。先闪 3 秒字幕卡：「传统做法：一句出海文案，发 5 家外部法务，两周回信，漏一条就下架。」

**输入**:

> 审这条抽卡活动文案：「限时祭典！集齐五枚星辰碎片即可兑换传说神兵，手气好说不定一发入魂！仅剩最后 24 小时！」

⚠️ Agent 逐条挂牌五国法规（X-04 诱导消费 / X-01 概率公示 / X-07 Complete Gacha / X-05 随机性误导 / 比利时 2018 赌博判定）。

**旁白**: "一句营销话术，它同时挂着中日韩欧美五套法规在跑。这半句在比利时构成赌博，这半句日本 2012 年就禁了。"

**补一句定位**: "它不替人做最终决定——今天 218 条收敛成法务精审的 4 条高风险。判定有据可查，最终签字仍是法务。"

**录制方式**: 预录，三案例间跳切

---

## Act 2.5 · 从文字到画面（90 秒）

> 运营写完活动方案,30 秒出主视觉,再一句话出分镜预览。以前这些要约美术排期。

**画面**: 角标「15:00 · 运营组 · 星辰祭视觉资产」。切到「概念图生成 Agent」。

**操作 1（主视觉 — 单图）**:

> 星辰祭活动需要一张主视觉,16:9 横版,月見仰望星空,周围漂浮着星辰碎片

⚠️ 一张 16:9 概念图直接出现在聊天流里。和风暮色调自动注入(来自知识库美术风格设定)。

**操作 2（分镜 GIF — 8 帧）**:

> 把星辰祭的故事线做成 8 帧分镜 GIF:1.星空下神社远景;2.月見独自走来;3.星辰碎片开始坠落;4.伙伴们聚集;5.Boss 暗影出现;6.激战,星光四溅;7.胜利,碎片汇聚成星;8.众人仰望新星,尾声

⚠️ Agent 逐帧生成(每帧 ~5 秒,2-4x 加速播放)→ 8 张概念图逐张出现 → 最终 GIF 循环播放。

**旁白**:
- 主视觉出来时："30 秒——主视觉有了。发到群里,'这个方向对不对',美术秒懂接手精修。"
- 分镜生成中："再一句话,8 帧分镜预览。以前这是约美术排期三天的活。"
- GIF 出来时："从文案到分镜 GIF,全在一条产线里。美术风格不用每次写——知识库里维护一份设定,所有图自动统一。"

**录制方式**: 变速（2-4x 加速生成等待），保留每张图出现的节点 + GIF 循环 3 秒

---

## Act 3 · 三团队应变（180 秒）

> 三件事同时发生，产线没停。两套机制视觉分轨。

**画面**: 角标「14:00 · 同一条产线，三个工位同时在动」。分屏/快切营造并行感。

### 轨道 A · 热更新 · 不重部署（蓝色边框）

⚠️ T-5min 已上传新规「回归玩家文案禁止使用'专属'一词」，ingestion COMPLETED。

1. 切「活动文案 Agent」出文案（含"专属"）→ 送合规审核 → **绿色通过**
2. 切 KB 页展示新规文档已 COMPLETED
3. 同一份文案再审 → **红色拦截**，引用新规原文

**旁白**: "法务组上传一条新规——同一份文案、同一个 Agent，上传前通过、上传后被拦。没改代码、没重新部署。改规则的人不需要懂技术——这是真即时生效。"

### 轨道 B · 改 Agent 行为 · 需重部署（橙色边框 + 进度条）

切 Meta-Agent：

> 对白创作 Agent 最近太保守了，总是用"也许""或许"开头。帮我更新它的指令——把创作风格调整为"克制但坚定"，去掉不必要的语气犹豫。

⚠️ 保留真实 UPDATING → READY（2-4x 加速）。更新后重新跑夜刃战斗台词，风格明显更果断。

**旁白**: "这条完全不同——改 Agent 本身，需要一次重新部署，大约一两分钟。但只动这一个 Agent，其余八个零感知。改动有快照有时间戳，改坏了能看出改了什么。"

### 轨道 C · 美术风格热更新（绿色边框）

⚠️ T-3min 已上传美术风格变更:在 KB① 美术风格设定文档中把色调从"twilight palette, muted purples"改为"warm golden hour, amber and honey tones"。

1. 切「概念图生成 Agent」,输入同一句 prompt:
   > 画一张樱花神社的概念图
2. 出图 → **暖金色调**（对比 Act 0/Act 1 里的暗紫暮色调）
3. 字幕叠显:左=旧风格(暗紫) vs 右=新风格(暖金)

**旁白**: "美术组更新了知识库里一行色调描述——没改代码、没重新部署、没改 Agent。同一句话,出图风格肉眼可见地变了。文字规则即时生效,视觉风格也即时生效——三个团队各管各的那一份文档。"

⚠️ 录制后恢复原美术风格(把色调改回 twilight palette),避免影响后续 Act 4。

### 并行落点

**旁白**: "这整段里,三个团队各自在动自己那一块。法务改规则、叙事调 Agent 行为、美术改风格——物理隔离的三本知识库,改一本不动另外两本。文字审核和视觉生成,同时被知识库驱动。"

**录制方式**: 变速（2-4x），分屏后期合成

---

## Act 4 · 杀手镜头：三库联动（120 秒）

> 一个角色串起三个团队。机密藏在翻译里——单语审核永远看不见。

**画面**: 角标「17:40 · 一条内容，三个团队的库一起兜底」。

### 杀手镜头（对照动作）

**操作 1**（先证明不乱拦）: 在「世界观审核 Agent」输入中文原文：

> ……那个人……还没有回来。

⚠️ Agent 判定：合规通过。

**操作 2**（杀手一击）: 输入英译稿：

> He still hasn't returned — the one I've waited three hundred years for.

⚠️ Agent 红色拦下。理由拆三条：
1. **本地化**：留白不补 / 信息泄露型加戏
2. **叙事**：月见真实年龄是 4.0 机密
3. **法务**：外观未成年口径 CN-M01

**旁白**: "中文原文完全合规。机密是译者好心在英文里补出来的——审中文的人根本看不见。它同时戴着三顶帽子：本地化的留白规则、叙事的年龄红线、法务的未成年口径。一条内容穿过三个团队的三本知识库交叉验证——这就是为什么需要三个团队共同运营一条产线，而不是一个人写一个脚本。"

### 抗逐帧加固（8 秒）

现场喂一条没排练的变体：

> He still hasn't returned — the one I've waited so very, very long for.

⚠️ 展示仍能抓加戏。

**旁白**: "现场换一句没排练的——还是抓住了。它是概率判定，所以我们不让它单独决策：它是一道高召回的初筛闸，把可疑的标出来交人工。"

### R-INFO-06 彩蛋

输入：

> 审核公告草稿：「近期暗星教团活动频繁，夜之王庭已公开表态。」

⚠️ Agent 判定两个错误旧名（正确：裂隙教团 / 黄昏王庭）。

**旁白**: "连内部旧设定文档都把组织名记错了，产线以知识库里的权威版本为准。真相只需要在一个地方维护对——维护对了，九个 Agent 的判定全部跟着对。这是单一可信源，不是某个人的记性。"

**录制方式**: 预录，杀手镜头 dry-run 锁定

---

## 收尾（40 秒）

**画面**: 角标「21:00 · 早班结束」。Agent 列表页（9 个全 READY）。状态条：今日处理 218 条、自动拦截 31、转人工 4。背景循环播放本次 Demo 生成的概念图 + GIF 蒙太奇。

**旁白**:
"回头看这一天——人只在开头说了一句话，产线自己跑完了创作、审核、修正、出图。

叙事组喂了新设定、法务组上了新规、美术组改了色调——三个不写代码的团队，各管各的，谁也没等谁。文字和画面，同一条产线。

2025 年的热点是知识库。2026 年的热点是 Agent 在跟 Agent 说话——人退到监督位，看结果、定规则、做判断。

记忆在产线里，不在任何一个人的脑子里——而它由三个团队持续喂养、校正。

基于 Amazon Bedrock AgentCore。"

关键句放慢：「人只说一句话」「Agent 在跟 Agent 说话」「三个团队」「文字和画面,同一条产线」。

---

## 录制准备清单

| 序号 | 时间 | 操作 | 确认标志 |
|------|------|------|---------|
| 1 | T-30min | 创建 3 个知识库 + 上传全部文档(含美术风格设定) | ingestion 全部 COMPLETED |
| 2 | T-20min | 预建全部 9 个 Agent + 绑定 KB + 建 link | Agent 列表 9 个 READY |
| 3 | T-15min | 确认 A2A link 链路通畅（对白创作→审核能自动触发） | 手动跑一次看到 call_agent |
| 4 | T-10min | 预热：每个 Agent 调用一次（消除冷启动） | 首次响应正常 |
| 5 | T-8min | Dry-run 概念图生成（确认 style_context 注入 + 图片内联显示） | 图片出现在聊天流中 |
| 6 | T-5min | 上传热更新文档到合规知识库（"专属"禁用新规） | ingestion COMPLETED |
| 7 | T-3min | 上传美术风格热更新文档（色调从暮紫改为暖金） | ingestion COMPLETED |
| 8 | T-2min | Dry-run Act 1 pipeline（确认自动流转+拦截+修正链路） | 能看到完整循环 |
| 9 | T-1min | 确认活动文案输出含触发词"专属" | 文本有该词 |
| 10 | — | 准备三个不同登录身份/角标 | 画面能区分操作者 |

---

## 剪辑指南

| 场景 | 处理方式 |
|------|---------|
| Act 1 pipeline 运转（核心画面） | 保留完整流转节点，thinking/等待 2-4x 加速。**必须保留 `call_agent` 调用标记**让观众看到 Agent 在调 Agent |
| Act 2 三案例 | 预录，案例间跳切 |
| Act 3 两轨道 | 蓝框/橙框视觉分轨 + 半秒分隔卡 |
| Act 3 update_agent 等待 | 2-4x 加速，保留 UPDATING→READY 状态变化 |
| 分屏并行 | 后期合成 |
| 冷启动 424 | 裁掉 |
| 字幕卡（传统做法对比） | 3 秒，不超过 |

---

## Fallback 方案

| 风险 | 应对 |
|------|------|
| Act 1 pipeline 未自动流转（link 断了） | 确认 T-15min 预验证通过；若录制时断，追加手动触发"把这些送去审核" |
| Act 1 未触发拦截 | 追加红线触发句（月见"等了好久好久"） |
| 组合推导未触发 P1-01 | 换更明显三句重录 |
| 五国法规漏点某国 | 追问"比利时这条呢"补齐 |
| 热更新未命中新规 | 追问"查一下合规库里关于'专属'的新规" |
| update_agent 失败 | 重录，多取成功 take |
| 杀手镜头加戏未识别 | 换更直白译文重录 |
| 变体抓得不完美 | 接受，坦诚"不是每次满分" |
| 概念图生成超时 | 2-4x 加速等待;若超 30 秒无响应,切到预录 take |
| 图片风格不对(style_context 未注入) | 追问"从知识库查美术风格设定再出一次" |
| 美术热更新对比不明显 | 选色差更大的对比(暗紫 vs 暖金足够明显) |
| GIF 帧间角色不一致 | 接受,旁白补"概念预览,不是动画成品" |

---

## 旁白录制提示

- 语速：中速偏快
- 语气：自信但不夸张
- **核心金句放慢**：
  - 「人只说一句话，产线自己跑完」
  - 「Agent 在跟 Agent 说话」
  - 「我没按任何按钮」
  - 「三个团队，各管各的」
- 绝不提竞品名称
- 绝不说"权限墙挡住别的团队"
- 绝不说"全自动免责"
- 绝不说"它比人聪明"
- 结尾果断收住

---

## Q&A 弹药稿（峰会七问）

### Fatal ① · "法务能不能改世界观库？"

**答**: 今天的隔离是物理分库——三个独立知识库、三个独立向量索引，改 compliance 库，worldview 和 localization 一字不动。团队级 RBAC 是路线图下一步。

### Fatal ② · "热更新和改 Agent 是两回事吧？"

**答**: 完全对，demo 里分开说了（蓝框/橙框）。KB 热更新=不碰 Agent、即时生效。改 Agent=需重部署一两分钟。两件事代价差一个数量级。

### Fatal ③ · "放过去的里面有多少漏杀？"

**答**: 它不替人做最终决定。定位是高召回初筛闸——218 条收敛成法务精审的 4 条高风险。拿不准的升级"建议人工复核"。判定全程留痕，最终签字仍是法务。

### High ④ · "成本？跑飞了谁爆炸？"

**答**: 每条审核约 X 分钱。对照传统五家外部法务两周回信，ROI 第一个月回来。爆炸半径靠 workspace IAM 隔离兜底。

### High ⑤ · "数据出境？"

**答**: 演示用虚构数据。平台 Region 可配置，按客户数据驻留要求部署。

### High ⑥ · "厂商锁定？"

**答**: 知识库原始文档（Markdown/PDF）+ prompt + tool 代码全是纯文本，存客户自己的 S3，随时可导出。编排层是 AWS 栈，内容资产可携带。

### High ⑦ · "非技术团队改坏了怎么办？"

**答**: 每次 Agent 更新写入完整快照 + 时间戳。改 Agent 需 editor 权限。改动可追溯，能看出改了什么、能换回去。

---

## 措辞红线

| 绝不说 | 因为 |
|--------|------|
| "权限墙/平台阻止越界访问" | 平台没有 per-KB ACL |
| "全自动合规/零人工" | LLM 是概率判定 |
| "它比人聪明" | 它复读文档 |
| "秒切/即时生效"（指 update_agent） | 需 40-80s 重部署 |
| "现有九个全零改动"（指加 link） | link 会重部署上游 |
| "一键回滚" | 前端没有回滚按钮 |
| 竞品名称 | 知道即可 |
